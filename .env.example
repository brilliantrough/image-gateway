HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info
# Legacy single OpenAI mode only. Copying this file does not load it automatically;
# export or source these values before running the server.
OPENAI_API_KEY=your_openai_api_key
# Legacy single OpenAI mode base URL.
OPENAI_BASE_URL=https://api.openai.com/v1
# Optional multi-upstream config-router mode:
# UPSTREAM_CONFIG_PATH=config/upstreams.json
